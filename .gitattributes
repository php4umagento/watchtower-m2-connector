# Excluded from a composer/git-archive distribution (e.g. `composer create-project`
# or a tagged release tarball), but still tracked in the repository itself.
/Test export-ignore
/.gitattributes export-ignore
/.gitignore export-ignore
